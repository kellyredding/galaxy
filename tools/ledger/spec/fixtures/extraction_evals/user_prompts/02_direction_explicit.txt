Always use trailing commas in multiline arrays and hashes. Never use single quotes for strings. When writing service objects, always inherit from ApplicationService and use the .call class method pattern — don't instantiate services directly.
