# macOS
.DS_Store

# Editor/IDE
.idea/
*.swp
*~
.vscode/

# Vendored dependencies (cloned locally, not committed)
# See GalaxyApp/SETUP.md for instructions to set up
GalaxyApp/Vendor/

# Xcode build output (from xcodebuild -derivedDataPath build)
GalaxyApp/build/

# Tool-specific ignores are in each tool's .gitignore
